<%= form_with(model: [@project, @task, comment], multipart: true) do |form| %>
  <p><%= form.label :content %><br>
    <%= form.text_field :content %>
  </p>
  <p>
    <%= form.label :attachment %><br>
    <%= form.file_field :attachment %>
  </p>
  <p>
    <%= form.submit %>
  </p>
<% end %>
